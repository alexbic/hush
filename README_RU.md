<div align="right">
  <b>🇷🇺 Русский</b> &nbsp;|&nbsp; <a href="README.md">🇬🇧 English</a> &nbsp;|&nbsp; <a href="README_ES.md">🇪🇸 Español</a>
</div>

<div align="center">

<img src="assets/hush_brand_full.png" alt="HUSH — Hear · Understand · Shape · Hand back" width="500">

<br>

**Говори. Отпусти. Готово.**

Голосовой ввод с постобработкой через LLM — локально, без серверов, без подписок.

<br>

[![macOS 13+](https://img.shields.io/badge/macOS-13%2B-black?logo=apple&logoColor=white)](https://www.apple.com/macos/)
[![Python 3.14](https://img.shields.io/badge/Python-3.14-3776AB?logo=python&logoColor=white)](https://python.org)
[![PyObjC](https://img.shields.io/badge/UI-PyObjC%20%2B%20AppKit-5856D6)](https://pyobjc.readthedocs.io)
[![License: MIT](https://img.shields.io/badge/License-MIT-22C55E)](LICENSE)

</div>

---

HUSH — это инструмент для тех, кто думает быстрее, чем печатает. Зажал хоткей, сказал вслух, отпустил — текст уже в нужном приложении. Опционально: прогнал через LLM, получил готовое письмо, список задач или отформатированную заметку. Распознавание речи работает полностью на устройстве через Apple Neural Engine — никакой телеметрии, никаких облачных микрофонов.

## Почему HUSH?

Идея возникла после знакомства с приложением [Spokenly](https://spokenly.app). Оно показало, что голосовой ввод с постобработкой через LLM — это действительно удобно. Но захотелось своего: без платных ограничений, без перегруженного интерфейса, под конкретные рабочие сценарии.

HUSH создавался и дорабатывался в боевом режиме — я пользуюсь им каждый день. Поэтому каждая деталь появилась из реальной потребности, а не ради галочки. Надеюсь, что он окажется удобным и для вас.

Всё можно настроить под себя: сценарии, провайдеры, темы. Никаких платных версий — только открытый код.

---

## Содержание

- [Как это работает](#как-это-работает)
- [Режимы работы](#режимы-работы)
- [Хоткеи](#хоткеи)
- [Сценарии постобработки](#сценарии-постобработки)
- [Редактор сценариев](#редактор-сценариев)
- [История транскрипций](#история-транскрипций)
- [Провайдеры LLM](#провайдеры-llm)
- [Цветовые темы](#цветовые-темы)
- [Установка](#установка)
- [API ключи](#api-ключи)
- [Приватность](#приватность)
- [Архитектура](#архитектура)
- [Поддержать проект](#поддержать-проект)

---

## Как это работает

HUSH — это приложение-аксессуар: нет иконки в Dock, только иконка в строке меню. Запустили — забыли о нём. Оно всегда готово принять голос в любом приложении.

**Под капотом:**

- Речь захватывается через `sounddevice` прямо в RAM
- Распознавание — NVIDIA Parakeet TDT 0.6B, скомпилированный под CoreML / Apple Neural Engine (~400 МБ, входит в поставку)
- При необходимости текст отправляется в LLM (локальный Ollama или облачный Anthropic / OpenAI / GLM)
- Результат вставляется в активное приложение через Accessibility API — без буфера обмена, без мигания

Первый запуск компилирует CoreML-модель под ваш конкретный чип. Это занимает до минуты и происходит один раз. Все последующие запуски — мгновенные.

---

## Режимы работы

### Тихий режим — удержание Fn

Самый быстрый способ диктовать. Не требует никаких дополнительных действий.

1. **Зажать** `Fn` → у края экрана появляется небольшой плавающий индикатор записи
2. **Говорить** — индикатор показывает, что идёт запись
3. **Отпустить** → фрагмент транскрибируется

Если нужно надиктовать несколько кусков:

- Зажать–отпустить несколько раз → фрагменты **накапливаются**
- Когда пауза длится **4 секунды** — начинается обработка через LLM
- Столбики индикатора визуализируют обратный отсчёт: зеленеют → краснеют
- **Right Shift+Enter** во время отсчёта → вставить немедленно без LLM
- **Hover** во время обработки → появляется кнопка прерывания (вставить сырой транскрипт)

Индикатор можно перетащить в любое место экрана — позиция сохраняется между сессиями.

---

### Полный режим — Fn+Control

Для сложных задач: диктовка по частям, выбор сценария, просмотр результата перед вставкой.

1. **Коротко нажать** `Fn+Control` → открывается большое окно (без немедленного начала записи)
2. **Зажать** `Fn` → записать фрагмент → **отпустить** → транскрипт появляется в окне как отдельный блок
3. Повторять сколько угодно — блоки накапливаются в окне
4. Выбрать нужный сценарий и нажать его кнопку — текст уйдёт в LLM
5. Кнопка **Insert** → вставить текущий текст в предыдущее активное приложение, окно закрывается
6. **Shift+Enter** → вставить и применить сценарий по умолчанию для полного режима, если он назначен

Если в настройках задан сценарий «по умолчанию для полного режима» (★), он применяется автоматически при `Shift+Enter`. Кнопка **Insert** в полном режиме всегда делает обычную вставку без запуска сценария.

---

### Расширенный режим — двойной клик по заголовку

Когда надиктовано много текста и его нужно спокойно прочитать, отредактировать или просто удобно видеть — главное окно можно развернуть.

- **Двойной клик** в любом месте заголовка главного окна → окно разворачивается до 640×680
- Повторный двойной клик → возврат к компактному размеру

В расширенном режиме вспомогательные панели (настройки, история, провайдеры, редактор) автоматически собираются в кластер 2×2 рядом с окном:

- Перетащить любую панель кластера → **весь кластер двигается вместе**
- **🔄** в настройках → сбросить кластер в позицию по умолчанию
- **🎯** → показать / скрыть все панели кластера

---

## Хоткеи

| Жест                             | Действие                                            |
| -------------------------------- | --------------------------------------------------- |
| Зажать `Fn`                      | Начать запись (тихий режим)                         |
| Отпустить `Fn`                   | Остановить запись, транскрибировать                 |
| Коротко нажать `Fn+Control`      | Открыть / закрыть полный режим                      |
| Зажать `Fn` в полном режиме      | Записать следующий блок                             |
| Right Shift+Enter во время отсчёта | Немедленная вставка без LLM                       |
| Кнопка Insert в полном режиме    | Обычная вставка без сценария                        |
| Shift+Enter в полном режиме      | Вставить и применить дефолтный сценарий, если задан |
| Навести курсор на индикатор обработки | Показать действие прерывания и вставки сырого текста |
| Двойной клик по заголовку        | Развернуть / свернуть главное окно                  |

---

## Сценарии постобработки

Сценарии — это настраиваемые системные промпты, которые LLM применяет к расшифрованному тексту. Каждый сценарий — это кнопка в интерфейсе. Можно редактировать встроенные и создавать свои.

Конфигурация хранится в `~/.config/hush/scenarios.json`.

### Встроенные сценарии

| Сценарий | Что делает |
|----------|------------|
| **MAIN** | Умный форматировщик: определяет тип текста (промпт, задачи, письмо, заметка) и применяет соответствующее форматирование; убирает слова-паразиты и оговорки |
| **ЧИСТКА** | Расставляет пунктуацию, заглавные буквы, типографские кавычки («»); чистит паразиты без изменения смысла |
| **ПИСЬМО** | Оформляет текст как деловое письмо: обращение, структура, подпись |
| **Задачи** | Преобразует поток мыслей в список задач с чекбоксами в формате Markdown |
| **MD** | Форматирует как Markdown с заголовками, списками и блоками кода |

> Сценарий без промпта вставляет сырой транскрипт — удобно как «кнопка быстрой вставки».

### Флаги сценариев

- **`тихий режим`** — назначить этот сценарий по умолчанию для тихого режима (только один)
- **`по умолчанию ★`** — назначить этот сценарий по умолчанию для полного режима (только один)

---

## Редактор сценариев

Открыть: ⚙ Настройки → нажать на любой сценарий.

| Поле                        | Описание                                                                     |
| --------------------------- | ---------------------------------------------------------------------------- |
| **Название** (RU / EN / ES) | До 6 символов — отображается на кнопке сценария                              |
| **Модель**                  | Переопределить LLM для этого конкретного сценария (пусто = авто)             |
| **Промпт**                  | Системная инструкция; расшифрованный текст добавляется в конец автоматически |
| **Тихий режим**             | Назначить сценарий по умолчанию для тихого режима                            |
| **По умолчанию ★**          | Назначить сценарий по умолчанию для полного режима                           |

При переключении между сценариями с несохранёнными изменениями HUSH спросит: **Сохранить / Отменить**.

---

## История транскрипций

Последние **50 транскрипций** сохраняются автоматически в `~/.config/hush/history.json`.

Панель истории (🕐) содержит три вкладки:

| Вкладка | Содержание |
|---------|------------|
| **Все** | Все блоки в хронологическом порядке |
| **Сессии** | Группировка: несколько фрагментов одной сессии объединены в одну запись |
| **Блоки** | Только отдельные фрагменты |

**Что можно сделать с историей:**

- Клик по элементу → добавить его как новый блок в текущую сессию полного режима
- Чекбоксы → множественный выбор
- Кнопки при множественном выборе: **Удалить** / **Объединить** / **Добавить** / **Заменить**

Панель остаётся открытой после вставки — можно последовательно выбирать и комбинировать несколько записей.

---

## Провайдеры LLM

Настроить: ⚙ → **[КЛЮЧИ]**.

| Провайдер | Тип | Что нужно |
|-----------|-----|-----------|
| **Ollama** | Локально | Установленный [Ollama](https://ollama.ai) + модель (`ollama pull <model>`) |
| **Anthropic** | Облако | API ключ (`sk-ant-...`) |
| **OpenAI** | Облако | API ключ (совместим с любым OpenAI-совместимым API) |
| **GLM** | Облако | API ключ Zhipu GLM-4 |

Панель **[КЛЮЧИ]** теперь работает как набор карточек провайдеров. У каждого провайдера всегда виден заголовок, а по клику карточка раскрывается вниз и позволяет настроить:

- endpoint
- API ключ
- протокол
- модель по умолчанию

В свернутом состоянии, если ключ сохранен, показывается его замаскированный фрагмент.

Дополнительные OpenAI-совместимые endpoint можно добавлять в `~/.config/hush/providers.json` по schema v2 и дальше использовать в той же логике провайдеров.

### Выбор модели в сценарии

Модель для каждого сценария выбирается в редакторе сценариев через два выпадающих списка:

1. **Провайдер** — выбрать из настроенных и доступных
2. **Модель** — список автоматически заполняется моделями, доступными у выбранного провайдера, а также вручную сохраненной моделью по умолчанию из карточки провайдера

В списке показываются человекочитаемые label-имена провайдеров, но внутри сценарий сохраняется как `provider_id:model_name`. Модель по умолчанию из карточки провайдера помечается `★` и выбирается первой. Если модель не выбрана, HUSH использует авто-стратегию: сначала пробует Ollama, при недоступности — Anthropic.

---

## Цветовые темы

8 встроенных тем, переключаются в настройках (⚙ → Тема). Все открытые панели — настройки, редактор сценариев, история — обновляются мгновенно.

| Тема | Фон | Акцент |
|------|-----|--------|
| **emerald** | Тёмно-зелёный | Ярко-зелёный |
| **ocean** | Тёмно-синий | Голубой |
| **neon** | Тёмно-фиолетовый | Маджента |
| **gold** | Тёмно-янтарный | Жёлтый |
| **paper** | Кремовый | Тёмно-зелёный |
| **sky** | Светло-голубой | Тёмно-синий |
| **sand** | Тёплый бежевый | Коричневый |
| **arctic** | Ледяной белый | Бирюзовый |

---

## Установка

### Требования

- **macOS 13+** (Ventura или новее)
- **Python 3.14** — `brew install python@3.14`
- **Разрешение Accessibility** — для автоматической вставки текста (запрашивается при первом запуске)
- **Разрешение Microphone** — запрашивается при первом запуске

Опционально, для LLM-сценариев:
- [Ollama](https://ollama.ai) с загруженной моделью
- API ключи Anthropic / OpenAI / GLM

### Шаги

```bash
git clone https://github.com/alexbic/hush.git
cd hush

# Установить зависимости
pip3.14 install pyobjc sounddevice pynput anthropic openai

# Собрать app bundle
bash build_app.sh

# Запустить
open HUSH.app
```

При первом запуске HUSH автоматически:
1. Копирует `parakeet-cli` в `~/.local/bin/`
2. Размещает CoreML-модель в `~/.local/share/hush/`
3. Компилирует модель под ваш чип (ANE) — происходит один раз, кеш сохраняется между обновлениями

После этого HUSH появится в строке меню и будет готов к работе.

---

## API ключи

**Вариант 1 — через интерфейс:** ⚙ → [КЛЮЧИ] → вписать ключи в поля.

**Вариант 2 — через файл** `~/.hush_env`:

```bash
export ANTHROPIC_API_KEY="sk-ant-..."
export OPENAI_API_KEY="sk-..."
export OLLAMA_BASE_URL="http://localhost:11434"   # по умолчанию
export GLM_API_KEY="..."
```

---

## Приватность

- Всё распознавание речи работает **локально** — Parakeet TDT исполняется на устройстве через CoreML / Apple Neural Engine
- Аудио не записывается на диск и не сохраняется дольше одной сессии
- В облачный LLM уходит **только текст** — и только если вы сами настроили облачный сценарий (Anthropic / OpenAI / GLM)
- При использовании Ollama — вся обработка остаётся на вашем компьютере

---

## Архитектура

HUSH написан на Python 3.14 с нативным UI через PyObjC + AppKit. Никакого Electron, никаких веб-технологий — всё нативное.

```
main.py          Хоткеи, жизненный цикл сессии, вставка текста
overlay.py       Весь UI: тихий режим, полный режим, история, темы
recorder.py      Захват аудио (sounddevice)
transcriber.py   Обёртка над Parakeet TDT subprocess + CoreML warmup
processor.py     Маршрутизация LLM (Ollama / Anthropic / OpenAI / GLM)
injector.py      Вставка текста через Accessibility API
config.py        Пути, API ключи, константы
build_app.sh     Сборка HUSH.app bundle
```

---

## Языки

Все мои проекты — сайт [alexbic.net](https://alexbic.net), инструменты, приложения — я стараюсь делать на трёх языках: **русском, английском и испанском**. Хочется, чтобы люди с разным языковым background могли пользоваться этими решениями без барьеров. HUSH — не исключение.

Интерфейс и сценарии переведены на все три языка. Сам я тестировал распознавание преимущественно на русском — это мой родной язык. Parakeet TDT заявлен как многоязычная модель, но насколько хорошо он справляется с английским или испанским на практике — честно, не знаю.

Если вы работаете на английском или испанском и готовы попробовать — буду очень рад обратной связи. Как Parakeet слышит ваш язык, что работает, что нет, какие сценарии вы написали — открывайте [Issue](https://github.com/alexbic/hush/issues) или пишите напрямую. Это поможет сделать HUSH лучше для всех.

---

## Поддержать проект

HUSH — бесплатный проект с открытым исходным кодом. В его разработке активно использовался [Claude Code](https://claude.ai/code) — и это не стыдно признавать: языковые модели сегодня такой же инструмент разработчика, как компилятор или редактор кода. Мы ценим своё время — и ценим ваше.

Если HUSH оказался полезным, если он сэкономил вам хотя бы несколько минут в день — мы будем рады любой поддержке. Это помогает продолжать делать интересные вещи.

<div align="center">

<a href="https://pay.alexbic.net/?mode=donate"><img src="assets/wallet.png" width="300" alt="Поддержать проект"></a>

**[Поддержать проект](https://pay.alexbic.net/?mode=donate)**

</div>

Спасибо. Правда.

---

## История версий

### v3.0 — 05.08.2026
- Базовая схема захвата переведена на `Fn` и `Fn+Control`
- Кнопка Insert в полном режиме теперь всегда делает обычную вставку без автозапуска дефолтного сценария
- Логика настройки провайдеров переведена на раскрывающиеся карточки с моделью по умолчанию

### v1.3 — 11.07.2026

**Стабильность: parakeet-cli больше не зависает после сна компьютера**

- `parakeet-cli` работает через ANE/CoreML; если процесс переживает переход системы в сон, его ANE-контекст рвётся, и после пробуждения любая операция на нём блокируется навсегда — до жёсткого таймаута в 360 с.
- `_SleepObserver` теперь убивает активный `parakeet-cli` (транскрипцию и прогрев) в `systemWillSleep_`, и подстраховывается тем же в `systemDidWake_` перед переинициализацией PortAudio — ни один процесс не переживает сон с незавершённым ANE-контекстом.
- `transcriber.cancel()` теперь завершает и `_current_proc`, и `_warmup_proc` (раньше — только текущую транскрипцию), что заодно чинит скрытую версию старой гонки «два parakeet-cli конкурируют за ANE» при отмене во время активного прогрева.
- Убийство процесса по таймауту/отмене теперь пишется в `/tmp/vi_transcribe.log` вместо тихого возврата пустой строки — зависания такого рода больше не остаются без следа в логах.

---

### v1.2 — 01.07.2026

**Стабильность: исправлен краш на macOS 26 (Tahoe)**

- Заменён `pynput`-листенер на глобальные мониторы `NSEvent`. `pynput` вызывал `TSMGetInputSourceProperty` из фонового потока; macOS 26 добавил принудительную проверку `dispatch_assert_queue` для этого API и HUSH падал при каждом отключении монитора + переходе в сон. `NSEvent`-мониторы работают в главном RunLoop — нет фонового потока, нет TSM, нет краша.
- Исправлен краш (heap corruption / SIGTRAP) при быстрых повторных нажатиях Alt: `_state["stream"]` теперь очищается сразу при событии отпускания клавиши, а не внутри рабочего потока. Это предотвращало вызов двух потоков на `recorder.stop()` одного PortAudio-стрима.

**Стабильность: хоткей больше не зависает**

- `_cancel_all()` (Alt+Shift) теперь сразу сбрасывает `_processing_locked`, чтобы следующее нажатие Alt никогда не блокировалось пока LLM-запрос завис или ждёт таймаута.
- Таймаут запроса к Ollama уменьшен со 120 с до 25 с; результат отбрасывается если сессия была отменена пока запрос выполнялся.
- `recorder.stop()` получил жёсткий таймаут 5 секунд на `stream.stop()/close()` — PortAudio может зависнуть при смене аудиоустройства; раньше это замораживало хоткей до разблокировки стрима.
- Ошибки `recorder.start()` (устройство недоступно и т.п.) перехватываются и оверлей корректно скрывается вместо того чтобы HUSH застрял в UI «запись».
- Дебаунс 800 мс на `_cancel_all()` предотвращает срабатывание нескольких отмен от очереди нажатий Alt+Shift.

**Производительность: быстрая транскрипция после простоя**

- Parakeet теперь прогревается тихим WAV 0.5 с каждые 30 секунд чтобы модель CoreML оставалась горячей в кэше Apple Neural Engine (TTL ≈ 44 с). Без прогрева первая транскрипция после любой паузы дольше 44 с занимала 60–90 с вместо ~1 с.
- `transcriber.cancel()` использует SIGTERM → 1 с ожидания → SIGKILL только если не завершился. SIGKILL инвалидировал кэш скомпилированной модели ANE; SIGTERM позволяет CoreML корректно освободить его. После каждой отмены также запускается прогрев.
- Исправлено несколько гонок, из-за которых два процесса `parakeet-cli` конкурировали за ANE и оба тормозили до 60+ с:
  - `Popen` прогрева теперь выполняется под `_proc_lock` — `transcribe()` не может прочитать `_warmup_proc = None` в промежутке между проверкой и сохранением.
  - Периодический прогрев пропускается если уже выполняется прогрев или реальная транскрипция.
  - `transcribe()` ждёт завершения запущенного прогрева до 90 с (вместо убийства через 1.5 с). При холодном ANE прогрев длится ≈ 60 с; убивать его и начинать заново бессмысленно — ожидание означает, что реальная транскрипция стартует с тёплым ANE и занимает ~1 с.

**Мелкие исправления**

- OpenAI-совместимый API: используется `max_completion_tokens` для серии моделей `o1`/`o3`/`o4` (`max_tokens` возвращает HTTP 400 для этих моделей).
- Панель прерывания: симметричный горизонтальный отступ 8 пикселей.

---

### v1.1 — 25.06.2026

**Система гридов для панелей — переписана с нуля**

- Сброс (`[🔄]`) теперь размещает все окна как группу по центру экрана. Паттерн выбирается автоматически по размеру экрана: крест 3×3 для большого, 3×2 для среднего (большинство MacBook), 2×2 для компактного.
- После сброса панели никогда не перекрываются: позиции вычисляются геометрически до открытия, затем вся группа сдвигается на экран.
- Новое правило размещения: диагональная ячейка используется только если хотя бы одна соседняя по грани уже занята — окна не соединяются «только углом».
- При перетаскивании за край экрана панель перестраивается на противоположную сторону, а не строит лишнюю 4-ю горизонтальную колонку.

**Единообразие интерфейса**

- Все кнопки приведены к единому стилю `[ПРОПИСНЫМИ]` в квадратных скобках (RU/EN/ES).
- Цвет кнопки `[СЦЕНАРИЙ]` теперь зависит от темы: `C_YEL` задаётся отдельно для каждой темы и обновляется при переключении. Раньше жёлтый был хардкоженным и плохо читался на светлых темах.

**Мониторинг провайдеров**

- Кнопки сценариев становятся красными если назначенный LLM-провайдер недоступен. Статус проверяется каждые 30 с в фоновом потоке; список сценариев обновляется автоматически при каждом изменении статуса.

---

### v1.0 — 23.06.2026

Первый публичный релиз. Подробности — в [заметках к релизу v1.0](https://github.com/alexbic/hush/releases/tag/v1.0).

---

## Лицензия

[MIT](LICENSE) © 2026 Alexander Bikmukhametov
